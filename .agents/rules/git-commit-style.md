# Git Commit Style Rule

When staging, committing, and pushing code to Git repositories:
- Use standard, professional Conventional Commit messages (e.g., `feat: add interactive seat map grid`, `fix: update authentication filter`).
- **NEVER** mention phase numbers or use the word "phase" in Git commit messages. Keep commit messages strictly focused on feature descriptions, bug fixes, or refactoring details.
